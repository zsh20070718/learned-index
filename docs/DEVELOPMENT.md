# 开发记录

## 2026-09-26：统一静态点查询入口

- 将“数据集位置 + 实现位置 → 详细报告”接成第一条可运行流程。新增 `scripts/benchmark.py`、独立 C++17 runner 和 map/排序数组两个示例适配包，统一入口无需下载 TLI/GRE。
- 单文件 reader 覆盖原始行字节、JSONL 指定字符串字段和旧 TLI 二进制；入口负责排序去重、规模/键性质统计和物理/逻辑哈希，不沿用旧工具的键长或 NUL 过滤限制。
- 适配包独立构建，多实现复用同一数据与负载。先检查能力并独立校验，再以新实例重复测吞吐量、单独测延迟；失败与不支持项保留状态。
- 输出中文 `report.md`、完整 `report.json`、汇总与原始样本 CSV；支持显式准备内存预算、构建和执行超时，已有结果目录不覆盖。
- 当前仅覆盖单线程静态点查询；写入/扫描/范围负载、分片与外部 reader 插件、完整适配器认证及正式性能实验仍待推进。旧 TLI 路径继续保留。
- 复现方法与验证记录见 [本周改进记录](WEEKLY_UPDATE_2026-09-26.md)，操作细节见 [统一入口说明](UNIFIED_BENCHMARK.md)。

## 2026-09-20：确定数据与实现之间的评测边界

- 明确产品目标：用户指定外部数据集和实现的位置，统一脚本完成评测并输出详细报告。统一入口仍待实现。
- 数据侧尽量宽松：不要求原始数据满足固定格式、排序、唯一性或字节数上限；读取/取键通过 reader，排序去重和元数据由 benchmark 完成并报告。
- 实现侧确定 C++17 接口、工厂与适配包发现约定，明确命中/缺失、重复插入、更新删除、scan 与 range_sum、所有权、能力声明、计时和内存口径。
- 新增 `docs/DATASET_CONTRACT.md`、`docs/INDEX_CONTRACT.md` 和独立签名头文件 `src/index_contract.h`；旧 TLI runner 和被测索引尚未迁移，当前工具的限制不提升为通用数据限制。
- 验证：独立 C++17 头文件在严格警告下通过语法检查，核对工厂/查询/扫描类型；六份相关文档的本地链接与代码块检查通过。本次不宣称统一入口或适配器已通过运行测试。

## 2026-09-20：真实数据多样性与 Zipf 查询

- 从 TLI 内嵌 Wormhole 的来源链接获取完整英文词表和 MemeTracker phrase-cluster 文件，新增固定大小、SHA-256、版本和出处的下载清单。原始数据保留本地。
- 提取英文词条、真实 URL、自然语言短语三类键；流式扫描全文件，按固定种子 hash 抽取唯一键，避免词典前缀偏差或重复 URL 加权。记录过滤数量、键长和相邻公共前缀统计。
- Smoke 可接收多份真实数据并始终保留 random/prefix 合成对照，要求相同键数并校验元数据哈希。
- 新增有限 Zipf 查询及 50% 未命中组合；指数可控，排名绑定字节排序，和真实流量热度明确区分。默认合成 smoke 扩展到 60 组，三类真实数据加入后为 150 组。
- 实测：完整下载约 235 MB；三类各 10,000 键、每负载 2,000 操作，150 组独立校验和吞吐量均通过，证据在 `results/smoke-f19n2phl/`。CTest 四个套件通过；下载入口从空缓存实测通过，另有离线下载校验、字段提取、抽样、统计和 Zipf 测试。
- MemeTracker 是官方 phrase-cluster 发布版，不等同于 TLI 的 `url_90M_string` 或 Wormhole 原始 m9urls 快照。小规模未绑核结果只验证执行链，尚不用于正式性能排名。

## 2026-09-20：扩展 benchmark 查询负载

- 新增 TLI 格式兼容的静态点查询生成器，补足上游字符串分支忽略未命中比例的限制；没有修改被测索引。
- 支持固定次数的未命中、uniform/热点访问及固定随机种子，缺失键在完整数据集上检查不存在。
- 新增五种点查询组合，批量实验从 32 扩展到 52 组；每份负载记录实际数量、热点源键分布、唯一键数及输入输出哈希。
- 比例使用有界十进制精确计算，包含 29% 整数边界和极端指数拒绝测试，避免配置比例与实际次数偏离。
- 验证：六项生成器测试通过，CTest 3/3、52 组校验及计时通过；最终本地证据为 `results/smoke-wjtg_y51/`，被测索引及上游源码未修改。
- 保留原有八种读写/区间负载；新增内容只扩展 benchmark，不增加索引算法。

## 2026-09-20：增强 benchmark 测量与校验

项目定位：开发字符串索引 benchmark，接入和比较已有实现，不开发索引算法。

- 为查询结果增加编译器屏障，防止无校验计时路径中的实际查询被优化掉；早期吞吐量不与新版直接比较。
- 独立 map 参照检查查询、每次插入后读回，并在操作完成后检查全部预期键值；通过故障注入测试校验器本身。
- 保留 ART/StdMap 两种比较项、两种键形态和八种负载，共 32 组。
- 新增有界规模参数、1–10 轮重复测量、逐轮样本、中位数及离散程度；记录数据和负载哈希以便复现。
- 验证：离线构建通过，CTest 2/2；32 组负载各重复 3 轮，共 96 个吞吐量样本，本地证据在 `results/smoke-md60ji3x/`。
- 后续推进已有学习型索引适配、未命中及偏斜访问负载、真实数据和统一测量口径。

## 2026-09-10：初始化 benchmark 工作区

本次由张圣皓发起，使用 Codex 协助完成初始 setup，并授权创建 GitHub public 仓库。

- 根据提供的研究说明确定 benchmark 方向；课程 PPT 用作数据类型与基础负载比例的参考。
- 固定 TLI/GRE 提交号，新增本地依赖准备、CMake 构建和小规模测试脚本。
- 新增独立的字符串执行入口与 StdMap 对照，复用 TLI 的 ART、数据格式、生成器、计时和校验实现。
- 将上游吞吐量与校验分开执行，并显式处理未知索引和验证失败。
- 当前工作范围及未完成的研究能力记录在 BENCHMARK.md，测试证据保留在本地 results/。

这是工作摘要，不是原始完整对话导出。原始对话保留在发起 setup 的 Codex 任务中；附件、会话内容、真实数据及机器信息不随 public 仓库发布。后续如需归档完整对话，应单独保留本地原始导出，并检查内容后再决定分享范围。
